#!/bin/sh

VIM=/usr/local/bin/nvim

if [ $# -ne 1 ]; then
    exec "$VIM" "$@"
fi

F="$1"
F=${F#[ab]/}
F=${F%:}
LINE=${F##*:}
if [ "$LINE" != "$F" ]; then
    F=${F%:*}
    exec "$VIM" "$F" "+$LINE"
fi
exec "$VIM" "$F"
